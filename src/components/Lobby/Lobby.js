import React from "react";
import Possessions from "../Possessions/Possessions";
const Lobby = () => {
    return (React.createElement("div", null,
        React.createElement(Possessions, null)));
};
export default Lobby;
